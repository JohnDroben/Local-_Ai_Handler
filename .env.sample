# Пример переменных окружения для .env
# OLLAMA_URL — адрес локального Ollama, используемый контейнерами.
# По умолчанию используется host.docker.internal:11434 через docker-compose.
OLLAMA_URL=http://host.docker.internal:11434

# MOCK_LLM — включить временный mock-режим (true/false). В mock режиме backend
# не обращается к Ollama и возвращает предсказуемые фиктивные ответы — удобно
# для разработки и тестирования UI без поднятого Ollama.
# Пример: MOCK_LLM=true
MOCK_LLM=false
